# MCP stdio transport

The stdio transport runs the MCP server as a child process; the client
exchanges JSON-RPC 2.0 messages over stdin/stdout. Local-only, single
client.

> Source: paraphrased from modelcontextprotocol.io docs (CC-BY-SA).
